# Loaded when using docker-compose.proxy.yml.
# mod_php does not reliably inherit Docker env; PassEnv exposes these to PHP so
# getenv('HTTP_PROXY') / ContentExtension curl use the SOCKS proxy.

PassEnv HTTP_PROXY
PassEnv HTTPS_PROXY
PassEnv ALL_PROXY
PassEnv NO_PROXY
